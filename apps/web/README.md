# Web (`apps/web`)

Public-facing website (Next.js app).

## Run

From repo root:

```bash
pnpm --filter web dev
```

This starts Next.js on `http://localhost:3002`.

## Status

This app currently contains starter content from the Turborepo/Next.js template. Integrate it with the API as needed.
